# Hermes Voice Terminal backend configuration template (no secrets committed).
# Copy to .env and fill in real values; .env itself must never be committed.

# Hermes integration (ТЗ раздел 21) — OpenAI-compatible endpoint.
HERMES_BASE_URL=
HERMES_API_KEY=
HERMES_MODEL=
HERMES_TIMEOUT=120

# STT abstraction (ТЗ раздел 20) — configurable OpenAI-compatible endpoint.
STT_BASE_URL=
STT_API_KEY=
STT_MODEL=
STT_TIMEOUT=60
